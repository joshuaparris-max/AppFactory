{
  "name": "appfactory",
  "version": "1.0.0",
  "description": "AppFactory: Multi-agent development framework for building applications safely and reliably",
  "main": "dist/lib/index.js",
  "types": "dist/lib/index.d.ts",
  "scripts": {
    "lint": "echo 'Linting would run here (configure with eslint, prettier, etc.)'",
    "build": "tsc -p tsconfig.json",
    "test": "npm run build && node dist/tests/generator.test.js",
    "qa": "npm run lint && npm run build && npm run test",
    "check": "npm run lint && npm run build && npm run test",
    "dev": "echo 'Development server would run here'",
    "format": "echo 'Code formatting would run here'",
    "clean": "node -e \"const fs=require('fs'); for (const dir of ['dist','node_modules','.turbo']) fs.rmSync(dir,{recursive:true,force:true});\""
  },
  "keywords": [
    "appfactory",
    "multi-agent",
    "development",
    "ai",
    "collaboration",
    "workflow",
    "safety",
    "generator"
  ],
  "author": "Josh Parris",
  "license": "MIT",
  "engines": {
    "node": ">=18.0.0",
    "npm": ">=9.0.0"
  },
  "devDependencies": {
    "typescript": "^5.9.3"
  }
}
